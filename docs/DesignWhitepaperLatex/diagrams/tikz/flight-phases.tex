% Flight-phase primer: altitude-vs-time sketch of one simulated flight (unitless axes).
% Phases: pad-hold clamp, boost, burnout, coast, apogee, ballistic descent to z < 0
% ("Nominal" termination). Recovery is a dashed not-yet-implemented branch.
% Sources: notes/sim.md sec. 4.2 (pad-hold clamp, terminateCondition, NoLiftoff),
% sec. 4.7 (TrajectoryStatistics); notes/motors.md sec. 4.2/4.4 (burnout, emptyMass).
\begin{tikzpicture}

  % --- axes (unitless sketch: t and z only, ground at z = 0) ---
  \draw[axis] (0,0) -- (13.9,0) node[below left=2pt and 0pt,font=\sffamily\small] {$t$};
  \draw[axis] (0,0) -- (0,6.05) node[left=2pt,font=\sffamily\small] {$z$};
  \node[flowlbl,anchor=west] at (12.35,0.22) {ground $z=0$};

  % --- phase stations (liftoff, burnout, apogee, ground crossing) ---
  \draw[station] (1.2,-0.7)  -- (1.2,2.05);
  \draw[station] (3.6,-0.7)  -- (3.6,3.2);
  \draw[station] (7.0,-0.7)  -- (7.0,5.42);
  \draw[station] (11.45,-0.7) -- (11.45,0);

  % --- phase extents under the time axis ---
  \draw[dimline] (0,-0.7)    -- (1.2,-0.7)   node[flowlbl,midway] {pad};
  \draw[dimline] (1.2,-0.7)  -- (3.6,-0.7)   node[flowlbl,midway] {boost (burn)};
  \draw[dimline] (3.6,-0.7)  -- (7.0,-0.7)   node[flowlbl,midway] {coast};
  \draw[dimline] (7.0,-0.7)  -- (11.45,-0.7) node[flowlbl,midway] {descent (ballistic)};

  % --- trajectory: flat pad-hold segment, then the smooth flight arc ---
  \draw[qrBlue,line width=1pt] (0,0) -- (1.2,0);
  \draw[qrBlue,line width=1pt] plot [smooth,tension=0.6] coordinates
    {(1.2,0) (1.45,0.12) (1.7,0.35) (2.3,1.15) (3.0,2.2) (3.6,3.2)
     (4.5,4.15) (5.5,4.9) (6.3,5.28) (7.0,5.42) (7.7,5.25) (8.5,4.7)
     (9.4,3.7) (10.3,2.3) (11.1,0.8) (11.65,-0.4)};

  % --- recovery: the alternative branch that does not exist yet ---
  \draw[ghost,->] plot [smooth,tension=0.6] coordinates
    {(7.0,5.42) (8.0,4.75) (9.5,3.85) (11.0,2.95) (12.6,2.0) (13.4,1.55)};
  \node[flowlbl,align=center] at (12.35,2.95)
    {recovery descent\\ \emph{not yet implemented}};

  % --- force annotations ---
  \draw[->,qrAmber,line width=0.9pt] (2.45,1.35) -- (2.45,2.85);
  \node[flowlbl,anchor=south,align=center,text=qrAmber] at (2.6,2.95)
    {thrust $F(t)$\\ along $+z$};
  \draw[->,qrGray,line width=0.7pt] (5.7,4.8) -- (5.7,3.95);
  \node[flowlbl,anchor=north] at (5.7,3.85) {gravity $+$ drag only};

  % --- event markers ---
  \node[cmdot] at (3.6,3.2) {};
  \node[cmdot] at (7.0,5.42) {};
  \node[cmdot] at (11.65,-0.4) {};
  \node[flowlbl,anchor=south,align=center] at (1.2,2.2)
    {liftoff\\ first $z>0$ step};
  \node[flowlbl,anchor=west,align=left] at (3.75,2.85)
    {burnout: $F\to 0$ past \texttt{getMaxTime()},\\
     motor mass frozen at \texttt{emptyMass}};
  \node[flowlbl,anchor=south,align=center] at (7.0,5.6)
    {apogee: $v_z = 0$\\ \texttt{maxAltitude}, \texttt{timeOfMaxAltitude}};

  % --- header: what is actually integrated ---
  \node[flowlbl,anchor=west] at (0.15,6.3)
    {3-DOF point mass: $\dot{x}=v,\ \ \dot{v}=F(t,x,v)\,/\,m(t)$};

  % --- callouts ---
  \node[umlnote,text width=33mm] (padnote) at (2.0,4.9)
    {pad-hold clamp: $z$ and $\vec{v}$ zeroed each step until thrust yields
     $z>0$; \texttt{NoLiftoff} abort if still below 1\,m after 3\,s};
  \draw[umldep] (padnote.south west) -- (0.6,0.15);

  \node[umlnote,text width=56mm,anchor=north] (nominalnote) at (10.9,-1.3)
    {no recovery model today: descent is ballistic through $z=0$;
     \texttt{terminateCondition}: $z<0 \wedge v_z<0$
     $\Rightarrow$ \texttt{TerminationReason::Nominal}};
  \draw[umldep] (nominalnote.north) -- (11.65,-0.4);

\end{tikzpicture}
