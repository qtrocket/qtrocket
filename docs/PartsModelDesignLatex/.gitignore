# --- self-contained tooling (fetched by `make tools`) ---
tools/plantuml.jar

# --- rendered diagram intermediates/outputs (regenerated by `make diagrams`) ---
diagrams/out/

# --- LaTeX build artefacts ---
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.toc
*.lof
*.lot
*.lol
*.synctex.gz
*.fff
*.ttt
*.xdv

# --- minted cache ---
_minted-*/
_minted/

# --- final PDF: keep tracked so reviewers can read it without a build ---
!PartsModel.pdf
