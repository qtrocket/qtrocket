{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug qtrocket (CodeLLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/gui/qtrocket",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "preLaunchTask": "cmake build (Debug)"
        }
    ]
}
